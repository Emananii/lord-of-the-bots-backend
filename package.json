{
  "name": "lord-of-the-bots-backend",
  "version": "1.0.0",
  "description": "JSON Server backend for Lord of the Bots app",
  "scripts": {
    "start": "json-server --watch db.json --port 8000"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
